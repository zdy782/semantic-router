# Simulator deployment: Uses mock-vllm instead of llm-katan (no GPU required)
# Use with: ./deploy-to-openshift.sh --simulator
---
# Deployment 1: Model-A (mock-vllm simulator)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-model-a
  namespace: vllm-semantic-router-system
  labels:
    app: vllm-model
    model: model-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-model
      model: model-a
  template:
    metadata:
      labels:
        app: vllm-model
        model: model-a
    spec:
      containers:
      - name: model-a
        image: image-registry.openshift-image-registry.svc:5000/vllm-semantic-router-system/mock-vllm:latest
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        # mock-vllm uses default CMD from Dockerfile (uvicorn on port 8000)
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
# Deployment 2: Model-B (mock-vllm simulator)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-model-b
  namespace: vllm-semantic-router-system
  labels:
    app: vllm-model
    model: model-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-model
      model: model-b
  template:
    metadata:
      labels:
        app: vllm-model
        model: model-b
    spec:
      containers:
      - name: model-b
        image: image-registry.openshift-image-registry.svc:5000/vllm-semantic-router-system/mock-vllm:latest
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        # mock-vllm uses default CMD from Dockerfile (uvicorn on port 8000)
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
# Deployment 3: Semantic Router + Envoy Proxy (same as standard deployment)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: semantic-router
  namespace: vllm-semantic-router-system
  labels:
    app: semantic-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: semantic-router
  template:
    metadata:
      labels:
        app: semantic-router
    spec:
      initContainers:
      - name: model-downloader
        image: python:3.11-slim
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -e
          echo "Installing Hugging Face Hub..."
          pip install --no-cache-dir --user huggingface_hub

          echo "Downloading models to persistent volume..."
          cd /app/models

          # Use Python API to download models
          python3 << 'PYEOF'
          import os
          from huggingface_hub import snapshot_download

          models = [
              ("llm-semantic-router/Vela-1.0-Encoder-307M-Domain", "Vela-1.0-Encoder-307M-Domain", "e18f9d3a91457249416e64bbc58236ebe350c7a5"),
              ("llm-semantic-router/Vela-1.0-Encoder-307M-PII", "Vela-1.0-Encoder-307M-PII", "6d3300c4bd7975f30a664503f6c725cf1fbbad48"),
              ("llm-semantic-router/Vela-1.0-Encoder-307M-Guard", "Vela-1.0-Encoder-307M-Guard", "ab27ec4efe2bdc45336df2d35abb6f687e41214e"),
              ("llm-semantic-router/Vela-1.0-Encoder-307M-Embedding", "Vela-1.0-Encoder-307M-Embedding", "4cd14064da151af4508d5ce1b1c2327f7c3fa30f")
          ]

          cache_dir = "/app/cache/hf"
          base_dir = "/app/models"

          for repo_id, local_dir_name, revision in models:
              local_dir = os.path.join(base_dir, local_dir_name)

              # Check if model already exists
              has_model = False
              if os.path.exists(local_dir):
                  for ext in ['.safetensors', '.bin', 'pytorch_model.']:
                      for root, dirs, files in os.walk(local_dir):
                          if any(ext in f for f in files):
                              has_model = True
                              break
                      if has_model:
                          break

              # A changed immutable release must not reuse stale file-presence checks.
              if not has_model or revision != "main":
                  print(f"Downloading {repo_id}...")
                  snapshot_download(
                      repo_id=repo_id,
                      revision=revision,
                      ignore_patterns=["reproduction/*", "reproducibility/*", "lora/*"],
                      local_dir=local_dir,
                      cache_dir=cache_dir
                  )
                  print(f"Downloaded {repo_id}")
              else:
                  print(f"{local_dir_name} already exists, skipping...")

          print("All models downloaded successfully!")
          PYEOF

          echo "Model download complete!"
          ls -la /app/models/

          echo "Setting proper permissions..."
          find /app/models -type f -exec chmod 644 {} \; 2>/dev/null || true
          find /app/models -type d -exec chmod 755 {} \; 2>/dev/null || true

          echo "Creating cache directories..."
          mkdir -p /app/cache/hf /app/cache/transformers /app/cache/sentence_transformers /app/cache/xdg /app/cache/bert
          chmod -R 777 /app/cache/ 2>/dev/null || true

          echo "Model download complete!"
        env:
        - name: HF_HUB_CACHE
          value: /app/cache/hf
        - name: HF_HOME
          value: /app/cache/hf
        - name: TRANSFORMERS_CACHE
          value: /app/cache/transformers
        - name: PIP_CACHE_DIR
          value: /tmp/pip_cache
        - name: PYTHONUSERBASE
          value: /tmp/python_user
        - name: PATH
          value: /tmp/python_user/bin:/usr/local/bin:/usr/bin:/bin
        resources:
          requests:
            memory: "2Gi"
            cpu: "250m"
          limits:
            memory: "4Gi"
            cpu: "500m"
        volumeMounts:
        - name: models-volume
          mountPath: /app/models
        - name: cache-volume
          mountPath: /app/cache
      containers:
      - name: semantic-router
        image: ghcr.io/vllm-project/semantic-router/extproc:latest
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        ports:
        - containerPort: 50051
          name: grpc
          protocol: TCP
        - containerPort: 9190
          name: metrics
          protocol: TCP
        - containerPort: 8080
          name: classify-api
          protocol: TCP
        env:
        - name: LD_LIBRARY_PATH
          value: "/app/lib"
        - name: HF_HOME
          value: "/app/cache/hf"
        - name: TRANSFORMERS_CACHE
          value: "/app/cache/transformers"
        - name: SENTENCE_TRANSFORMERS_HOME
          value: "/app/cache/sentence_transformers"
        - name: XDG_CACHE_HOME
          value: "/app/cache/xdg"
        - name: HOME
          value: "/tmp/home"
        volumeMounts:
        - name: config-volume
          mountPath: /app/config/config.yaml
          subPath: config.yaml
          readOnly: true
        - name: config-volume
          mountPath: /app/config/tools_db.json
          subPath: tools_db.json
          readOnly: true
        - name: models-volume
          mountPath: /app/models
        - name: cache-volume
          mountPath: /app/cache
        livenessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 50051
          initialDelaySeconds: 90
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            memory: "3Gi"
            cpu: "1"
          limits:
            memory: "6Gi"
            cpu: "2"
      - name: envoy-proxy
        image: envoyproxy/envoy:v1.35.3
        ports:
        - containerPort: 8801
          name: envoy-http
          protocol: TCP
        - containerPort: 19000
          name: envoy-admin
          protocol: TCP
        command: ["/usr/local/bin/envoy"]
        args: ["-c", "/etc/envoy/envoy.yaml", "--component-log-level", "ext_proc:info,router:info,http:info"]
        env:
        - name: loglevel
          value: "info"
        volumeMounts:
        - name: envoy-config-volume
          mountPath: /etc/envoy
          readOnly: true
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        livenessProbe:
          tcpSocket:
            port: 8801
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8801
          initialDelaySeconds: 10
          periodSeconds: 15
          timeoutSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: config-volume
        configMap:
          name: semantic-router-config
      - name: envoy-config-volume
        configMap:
          name: envoy-config
      - name: models-volume
        persistentVolumeClaim:
          claimName: semantic-router-models
      - name: cache-volume
        persistentVolumeClaim:
          claimName: semantic-router-cache
---
# Service for Model-A
apiVersion: v1
kind: Service
metadata:
  name: vllm-model-a
  namespace: vllm-semantic-router-system
  labels:
    app: vllm-model
    model: model-a
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  selector:
    app: vllm-model
    model: model-a
---
# Service for Model-B
apiVersion: v1
kind: Service
metadata:
  name: vllm-model-b
  namespace: vllm-semantic-router-system
  labels:
    app: vllm-model
    model: model-b
spec:
  ports:
  - name: http
    port: 8001        # Intentional: vSR config expects model-b on 8001
    targetPort: 8000  # Container port: mock-vllm always runs on 8000
  selector:
    app: vllm-model
    model: model-b
---
# Service for Semantic Router
apiVersion: v1
kind: Service
metadata:
  name: semantic-router
  namespace: vllm-semantic-router-system
  labels:
    app: semantic-router
spec:
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
  - name: api
    port: 8080
    targetPort: 8080
  - name: envoy-http
    port: 8801
    targetPort: 8801
  - name: envoy-admin
    port: 19000
    targetPort: 19000
  selector:
    app: semantic-router
---
# Service for Metrics
apiVersion: v1
kind: Service
metadata:
  name: semantic-router-metrics
  namespace: vllm-semantic-router-system
  labels:
    app: semantic-router
spec:
  ports:
  - name: metrics
    port: 9190
    targetPort: 9190
  selector:
    app: semantic-router
