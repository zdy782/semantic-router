---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: intelligentroutes.vllm.ai
spec:
  group: vllm.ai
  names:
    kind: IntelligentRoute
    listKind: IntelligentRouteList
    plural: intelligentroutes
    shortNames:
    - iroute
    singular: intelligentroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Number of decisions
      jsonPath: .status.statistics.decisions
      name: Decisions
      type: integer
    - description: Number of keyword signals
      jsonPath: .status.statistics.keywords
      name: Keywords
      type: integer
    - description: Number of embedding signals
      jsonPath: .status.statistics.embeddings
      name: Embeddings
      type: integer
    - description: Number of domain signals
      jsonPath: .status.statistics.domains
      name: Domains
      type: integer
    - description: Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IntelligentRoute defines intelligent routing rules and decisions
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IntelligentRouteSpec defines the desired state of IntelligentRoute
            properties:
              decisions:
                description: Decisions defines the routing decisions based on signal
                  combinations
                items:
                  description: Decision defines a routing decision based on rule combinations
                  properties:
                    description:
                      description: Description provides a human-readable description
                        of this decision
                      maxLength: 500
                      type: string
                    modelRefs:
                      description: ModelRefs defines the model references for this
                        decision (currently only one model is supported)
                      items:
                        description: ModelRef defines a model reference without score
                        properties:
                          loraName:
                            description: LoRAName is the name of the LoRA adapter
                              to use (must exist in the model's LoRAs)
                            maxLength: 100
                            type: string
                          model:
                            description: Model is the name of the model (must exist
                              in IntelligentPool)
                            maxLength: 100
                            minLength: 1
                            type: string
                          reasoningDescription:
                            description: ReasoningDescription provides context for
                              when to use reasoning
                            maxLength: 500
                            type: string
                          reasoningEffort:
                            description: ReasoningEffort selects an effort value supported
                              by the model family.
                            type: string
                          reasoningMode:
                            description: ReasoningMode selects an activation mode
                              supported by the model family.
                            enum:
                            - enabled
                            - disabled
                            - adaptive
                            type: string
                          useReasoning:
                            default: false
                            description: UseReasoning specifies whether to enable
                              reasoning mode for this model
                            type: boolean
                        required:
                        - model
                        type: object
                      maxItems: 1
                      minItems: 1
                      type: array
                    name:
                      description: Name is the unique identifier for this decision
                      maxLength: 100
                      minLength: 1
                      type: string
                    plugins:
                      description: Plugins defines the plugins to apply for this decision
                      items:
                        description: DecisionPlugin defines a plugin configuration
                          for a decision
                        properties:
                          configuration:
                            description: Configuration is the plugin-specific configuration
                              as a raw JSON object
                            x-kubernetes-preserve-unknown-fields: true
                          type:
                            description: |-
                              Type is the plugin type. response_cache is canonical; semantic-cache,
                              semantic_cache, and response-cache are deprecated aliases.
                            enum:
                            - context_compression
                            - fast_response
                            - hallucination
                            - header_mutation
                            - memory
                            - rag
                            - request_params
                            - response_jailbreak
                            - router_replay
                            - shadow_dispatch
                            - response_cache
                            - response-cache
                            - semantic_cache
                            - semantic-cache
                            - system_prompt
                            - tools
                            type: string
                        required:
                        - type
                        type: object
                      maxItems: 10
                      type: array
                    priority:
                      default: 0
                      description: |-
                        Priority defines the priority of this decision (higher values = higher priority)
                        Used when strategy is "priority"
                      format: int32
                      maximum: 1000
                      minimum: 0
                      type: integer
                    signals:
                      description: Signals defines the signal combination logic
                      properties:
                        conditions:
                          description: Conditions defines the list of signal conditions
                          items:
                            description: SignalCondition defines a single signal condition
                            properties:
                              name:
                                description: |-
                                  Name is the name of the signal to reference
                                  For fact_check type, use "needs_fact_check" to match queries that need fact verification
                                maxLength: 100
                                minLength: 1
                                type: string
                              type:
                                description: Type defines the type of signal (keyword/embedding/domain/fact_check/context/structure/conversation/event)
                                enum:
                                - keyword
                                - embedding
                                - domain
                                - fact_check
                                - context
                                - structure
                                - conversation
                                - event
                                type: string
                            required:
                            - name
                            - type
                            type: object
                          maxItems: 50
                          minItems: 1
                          type: array
                        on_unknown:
                          description: OnUnknown resolves a terminal unknown result
                            after the signal tree is evaluated.
                          enum:
                          - no_match
                          - match
                          - fail_request
                          type: string
                        operator:
                          description: |-
                            Operator defines the logical operator for combining conditions (AND/OR/NOT).
                            NOT is strictly unary: it takes exactly one child condition and negates its result.
                            Compose NOR/NAND by nesting NOT around OR/AND.
                          enum:
                          - AND
                          - OR
                          - NOT
                          type: string
                      required:
                      - conditions
                      - operator
                      type: object
                  required:
                  - modelRefs
                  - name
                  - signals
                  type: object
                maxItems: 100
                minItems: 1
                type: array
              signals:
                description: Signals defines signal extraction rules for routing decisions
                properties:
                  contextRules:
                    description: |-
                      FactCheckRules defines fact-check rules for signal classification
                      Similar to KeywordRules and EmbeddingRules, but based on ML model classification
                      Each rule has a name that can be referenced in decision conditions
                    items:
                      description: ContextRule defines a rule for context-based (token
                        count) classification
                      properties:
                        description:
                          description: Description provides human-readable explanation
                          maxLength: 500
                          type: string
                        maxTokens:
                          description: |-
                            MaxTokens is the inclusive maximum token count (supports K/M suffixes).
                            Omit it to make the band open-ended so every request at or above
                            MinTokens matches. Equal MinTokens and MaxTokens match exactly one count.
                          pattern: ^[0-9]+(\.[0-9]+)?[KMkm]?$
                          type: string
                        minTokens:
                          description: |-
                            MinTokens is the inclusive minimum token count (supports K/M suffixes).
                            Omit it to start the band at 0. At least one of MinTokens and MaxTokens
                            must be set.
                          pattern: ^[0-9]+(\.[0-9]+)?[KMkm]?$
                          type: string
                        name:
                          description: Name is the signal name (e.g., "high_token_count")
                          maxLength: 100
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  conversation:
                    description: Conversation defines conversation-shape routing signals.
                    items:
                      description: |-
                        ConversationSignal defines a conversation-shape routing signal.
                        It detects multi-turn history, developer instructions, tool presence, and
                        assistant-tool interaction patterns in the request.
                      properties:
                        description:
                          type: string
                        feature:
                          description: ConversationCRDFeature selects what to measure
                            (count / exists) and where.
                          properties:
                            source:
                              description: ConversationCRDSource identifies the message
                                role or tool-related entity to measure.
                              properties:
                                role:
                                  type: string
                                type:
                                  type: string
                              required:
                              - type
                              type: object
                            type:
                              type: string
                          required:
                          - source
                          - type
                          type: object
                        name:
                          type: string
                        predicate:
                          properties:
                            gt:
                              type: number
                            gte:
                              type: number
                            lt:
                              type: number
                            lte:
                              type: number
                          type: object
                      required:
                      - feature
                      - name
                      type: object
                    maxItems: 100
                    type: array
                  domains:
                    description: Domains defines MMLU domain categories for classification
                    items:
                      description: DomainSignal defines a domain category for classification
                      properties:
                        description:
                          description: Description provides a human-readable description
                            of this domain
                          maxLength: 500
                          type: string
                        name:
                          description: Name is the unique identifier for this domain
                          maxLength: 100
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    maxItems: 14
                    type: array
                  embeddings:
                    description: Embeddings defines embedding-based signal extraction
                      rules
                    items:
                      description: EmbeddingSignal defines an embedding-based signal
                        extraction rule
                      properties:
                        aggregationMethod:
                          default: max
                          description: AggregationMethod defines how to aggregate
                            multiple candidate similarities
                          enum:
                          - mean
                          - max
                          - any
                          type: string
                        candidates:
                          description: Candidates is the list of candidate phrases
                            for semantic matching
                          items:
                            type: string
                          maxItems: 100
                          minItems: 1
                          type: array
                        name:
                          description: Name is the unique identifier for this signal
                          maxLength: 100
                          minLength: 1
                          type: string
                        prototypeScoring:
                          description: |-
                            PrototypeScoring overrides the family's prototype construction and scoring.
                            An omitted object inherits the family; an empty object uses core defaults.
                          properties:
                            bestWeight:
                              description: BestWeight weights the best similarity
                                against top-M support.
                              type: number
                            clusterSimilarityThreshold:
                              description: ClusterSimilarityThreshold controls membership
                                in prototype clusters.
                              type: number
                            enabled:
                              description: Enabled controls clustering and the representative
                                cap, not aggregation.
                              type: boolean
                            marginThreshold:
                              description: MarginThreshold controls the winner-versus-runner-up
                                score margin.
                              type: number
                            maxPrototypes:
                              description: MaxPrototypes caps representatives when
                                clustering is enabled.
                              type: integer
                            topM:
                              description: TopM is the number of top similarities
                                used for support.
                              type: integer
                          type: object
                        queryModality:
                          description: |-
                            QueryModality declares which modality of the incoming request payload
                            the query embedding is computed from. Candidates always remain text;
                            the rule cosine-matches the text-anchor set against a query embedding
                            produced from the declared modality, all in the shared multimodal
                            embedding space.

                            "text"  (default, backward-compatible): query embedded from request text.
                            "image": query embedded from an image attachment (base64 data URI or
                                     path). Requires the multi-modal embedding model to be loaded
                                     via global.model_catalog.embeddings.semantic with
                                     embedding_config.model_type=multimodal.
                            "audio": query embedded from an audio attachment. Same requirement.

                            Omitting this field is equivalent to "text" so existing rules behave
                            identically.
                          enum:
                          - text
                          - image
                          - audio
                          type: string
                        threshold:
                          description: Threshold is the similarity threshold for matching
                            (0.0-1.0)
                          maximum: 1
                          minimum: 0
                          type: number
                      required:
                      - candidates
                      - name
                      - threshold
                      type: object
                    maxItems: 100
                    type: array
                  events:
                    description: Events defines structured event metadata routing
                      signals.
                    items:
                      description: |-
                        EventSignal defines a structured event metadata routing signal.
                        It detects event types, severities, action codes, and temporal urgency
                        from incident, audit, alert, or workflow payloads in the request.
                      properties:
                        actionCodes:
                          items:
                            type: string
                          type: array
                        description:
                          type: string
                        eventTypes:
                          items:
                            type: string
                          type: array
                        name:
                          type: string
                        severities:
                          items:
                            type: string
                          type: array
                        temporal:
                          type: boolean
                      required:
                      - name
                      type: object
                    maxItems: 100
                    type: array
                  factCheckRules:
                    items:
                      description: |-
                        FactCheckRule defines a rule for fact-check signal classification
                        The classifier determines if a query needs fact verification based on the ML model
                        Predefined signal names: "needs_fact_check", "no_fact_check_needed"
                        Threshold is read from hallucination_mitigation.fact_check_model.threshold
                      properties:
                        description:
                          description: Description provides human-readable explanation
                            of when this signal is triggered
                          type: string
                        name:
                          description: |-
                            Name is the signal name that can be referenced in decision rules
                            e.g., "needs_fact_check" or "no_fact_check_needed"
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  keywords:
                    description: Keywords defines keyword-based signal extraction
                      rules
                    items:
                      description: KeywordSignal defines a keyword-based signal extraction
                        rule
                      properties:
                        caseSensitive:
                          default: false
                          description: CaseSensitive specifies whether keyword matching
                            is case-sensitive
                          type: boolean
                        keywords:
                          description: Keywords is the list of keywords to match
                          items:
                            type: string
                          maxItems: 100
                          minItems: 1
                          type: array
                        name:
                          description: Name is the unique identifier for this rule
                            (also used as category name)
                          maxLength: 100
                          minLength: 1
                          type: string
                        operator:
                          description: Operator defines the logical operator for keywords
                            (AND/OR)
                          enum:
                          - AND
                          - OR
                          type: string
                      required:
                      - keywords
                      - name
                      - operator
                      type: object
                    maxItems: 100
                    type: array
                  structure:
                    description: Structure defines request-shape routing signals.
                    items:
                      properties:
                        description:
                          type: string
                        feature:
                          properties:
                            source:
                              properties:
                                caseSensitive:
                                  type: boolean
                                keywords:
                                  items:
                                    type: string
                                  type: array
                                pattern:
                                  type: string
                                sequences:
                                  items:
                                    items:
                                      type: string
                                    type: array
                                  type: array
                                type:
                                  type: string
                              required:
                              - type
                              type: object
                            type:
                              type: string
                          required:
                          - source
                          - type
                          type: object
                        name:
                          type: string
                        predicate:
                          properties:
                            gt:
                              type: number
                            gte:
                              type: number
                            lt:
                              type: number
                            lte:
                              type: number
                          type: object
                      required:
                      - feature
                      - name
                      type: object
                    maxItems: 100
                    type: array
                type: object
            required:
            - decisions
            type: object
          status:
            description: IntelligentRouteStatus defines the observed state of IntelligentRoute
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the IntelligentRoute's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed IntelligentRoute
                format: int64
                type: integer
              statistics:
                description: Statistics provides statistics about configured decisions
                  and signals
                properties:
                  decisions:
                    description: Decisions indicates the number of decisions
                    format: int32
                    type: integer
                  domains:
                    description: Domains indicates the number of domain signals
                    format: int32
                    type: integer
                  embeddings:
                    description: Embeddings indicates the number of embedding signals
                    format: int32
                    type: integer
                  keywords:
                    description: Keywords indicates the number of keyword signals
                    format: int32
                    type: integer
                required:
                - decisions
                - domains
                - embeddings
                - keywords
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
